[build]
jobs = 8

[target.aarch64-apple-darwin]
rustflags = ["-C", "target-cpu=native"]

[env]
# FFmpeg configuration (if using ffmpeg-next)
# FFMPEG_DIR = "/opt/homebrew"

[alias]
r = "run"
t = "test"
c = "check"
b = "build"
